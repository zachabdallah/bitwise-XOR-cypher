# XOR Cipher

key functions
```
generate_key: random key generation using uppercase/lowercase/special characters
passphrase_key: random key generation using KDF, deriving a key from a passphrase and then appending/prepending a randomly generated string (Salt)
```
